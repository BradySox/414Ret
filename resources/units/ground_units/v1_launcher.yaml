# A 1944 launch ramp: cheap, fixed, and inaccurate by design.
price: 20
# A launch ramp is a poured emplacement, not a vehicle -- the same shape as
# `hy_launcher`, whose flown evidence is recorded in its own yaml (0 m of movement
# plus a per-frame ground-AI levelling storm).
mobile: false
class: Missile
variants:
  V-1 Launch Ramp: null